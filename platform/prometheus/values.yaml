server:
  persistentVolume:
    enabled: true
    size: 10Gi
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 1000m
      memory: 1Gi

alertmanager:
  enabled: false #一旦offに

pushgateway:
  enabled: false

nodeExporter:
  enebled: true
